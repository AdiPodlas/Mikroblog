import React from 'react';
import './BlogEntry.css';


export const BlogEntry = ({ title }) => {
    return (
        <header className="blog-entry">
            <h1>{title}
                xyz
            </h1>
        </header>
    );
};

